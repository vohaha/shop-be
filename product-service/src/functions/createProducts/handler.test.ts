// TODO add tests
describe('Unit test: createProduct', () => {
  beforeEach(() => {
    jest.restoreAllMocks();
  });
  it('should return 201', async () => {
    expect({}).toStrictEqual({});
  });
});
